/**
 * Founding member program: first N signups get a lifetime-locked discount.
 * Total spots, coupon code, and discount % are exposed here so the landing
 * page, the API, and the welcome email all stay in sync.
 */
export const FOUNDING_TOTAL_SPOTS = 20;
export const FOUNDING_COUPON_CODE = "FOUNDER30";
export const FOUNDING_DISCOUNT    = "30%";
